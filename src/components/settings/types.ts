import type { Employee, WorklogType, Grade, CreateEmployeeDto, CreateGradeDto, CreateWorklogTypeDto, UpdateEmployeeDto, UpdateGradeDto, UpdateWorklogTypeDto } from '../../types';

export type Tab = 'employees' | 'worklogTypes' | 'grades';

export type {
  Employee,
  WorklogType,
  Grade,
  CreateEmployeeDto,
  CreateGradeDto,
  CreateWorklogTypeDto,
  UpdateEmployeeDto,
  UpdateGradeDto,
  UpdateWorklogTypeDto
}; 